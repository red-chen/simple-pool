# -*- mode: python -*-
# -*- coding: utf-8 -*-

Import('env')

env = env.Clone()

env.Append(LIBPATH=['#/lib/'])

obj = [
    'cJSON.c'
]

env.aStaticLibrary("cjson_static", obj) 
env.aSharedLibrary("cjson_shared", obj) 

env.aProgram('cjson_test', ['test.c'], LIBS=['cjson_static', 'm'])
