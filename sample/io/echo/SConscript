# -*- mode: python -*-
# -*- coding: utf-8 -*-

Import('env')

env.Append(CPPPATH=['#/include/'])

env.Append(LIBPATH=['#/lib/'])

env.aProgram('io_client', ['client.c'], LIBS=['simple_static', 'pthread'])
env.aProgram('io_server', ['server.c'], LIBS=['simple_static', 'pthread'])
